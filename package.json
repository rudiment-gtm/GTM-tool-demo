{
  "name": "gtm-tool-demo-api",
  "private": true,
  "type": "module",
  "description": "Vercel serverless functions (api/) for the GTM tool demo - kept separate from the frontend in exports/vercel.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
